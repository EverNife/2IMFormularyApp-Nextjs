// const withPWA = require("next-pwa");
// const runtimeCaching = require("next-pwa/cache");

module.exports = {
  // pwa: {
  //   disable: process.env.NODE_ENV === 'development',
  //   dest: "public",
  //   runtimeCaching,
  //   publicExcludes: process.env.NODE_ENV === 'development' ? ['!/**/*'] : ['!noprecache/**/*'],
  //   buildExcludes: process.env.NODE_ENV === 'development' ? [/chunks\/.*$/] : [],
  // },

  // Enable this to help debug when something crazy happens
  // webpack(webpackConfig) {
  //   return {
  //     ...webpackConfig,
  //     optimization: {
  //       minimize: false
  //     }
  //   };
  // }
};
